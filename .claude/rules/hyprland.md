---
paths:
  - ".config/hypr/**"
---
# Hyprland Config Rules
- Keybindings use `$mainMod = SUPER`. Format: `bind = $mainMod, KEY, action, args`.
- Environment variables: `env = NAME,VALUE` (comma-separated, no spaces around comma).
- After editing: reload with `hyprctl reload`. No restart needed.
